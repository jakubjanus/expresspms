extends layout

block content
    #wrapper
        #display
            include navi

            div.container.well
            
                h3 New project form

                form(method='post')

                 div.input-group                      
                    input(type='text', name='name', size='50', class='form-control', placeholder='name')
                 div
                    input(type='submit', value='Save', class='btn btn-primary')

        include footer